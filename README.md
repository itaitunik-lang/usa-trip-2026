# Yosemite ו‑Grand Circle — אתר הטיול

אתר חי לטיול 5–20 באוגוסט 2026.
**https://itaitunik-lang.github.io/usa-trip-2026/**

---

## מה יש באתר

תפריט נפתח מהלוגו בפינה השמאלית העליונה, עם שמונה מסכים:

| מסך | מה יש בו |
|---|---|
| 🗺️ **המסלול** | 16 ימים — ה‑road trip במערב וניו יורק ברצף אחד, עם תחזית, ניווט וסימון "עכשיו/הבא" |
| 📝 **משימות פתוחות** | מה עוד לא נסגר, לפי דחיפות. הסימון נשמר בדפדפן |
| ✈️ **טיסות** | כל הקטעים עם שעות, טרמינלים, כבודה, זמני המתנה ורשימת מה לוודא |
| 🆘 **חירום ומידע חיוני** | 911, טלפוני פארקים ומרפאות לפי אזור, איפה לתדלק, ואזורי זמן |
| 📖 **רקע על האתרים** | 19 אתרים עם רקע היסטורי, גיאולוגי ותרבותי — מקובץ לפי ימים |
| 🎒 **מה ללבוש** | המלצות לכל יום, נגזרות מאופי היום ומהתחזית בפועל |
| 🧳 **ציוד לארוז** | רשימה עם כמויות לכל 16 הימים, כולל ניו יורק |
| 💳 **הוצאות** | סיכום מהקבלות בתווית `USA TRIP` ב‑Gmail |

בתפריט יש גם **מפת כל המסלול**, **שיתוף** ו**הדפסה / שמירת PDF** — גיבוי לנייר למקרה שהטלפון מת.

---

## עובד בלי אינטרנט

בזיון, ברייס, אנטלופ ומוניומנט ואלי אין קליטה. `sw.js` שומר במכשיר את כל הדפים, הנתונים והאיורים בביקור הראשון, והאתר נפתח במלואו גם בלי רשת.

- **הקבצים הקבועים** נטענים מהמטמון קודם — פתיחה מיידית.
- **`data/*.json`** נטענים מהרשת קודם ונופלים למטמון — כדי שהסנכרון האוטומטי לא ייתקע על נתונים ישנים.
- **התחזית האחרונה נשמרת** ומוצגת כשאין רשת, במקום ליפול לממוצע הרב‑שנתי.

אחרי שינוי ב‑`index.html` צריך להעלות את מספר הגרסה ב‑`CACHE` שבתוך `sw.js`, אחרת דפדפנים שכבר ביקרו ימשיכו להציג את הגרסה הישנה.

---

## מבנה הקבצים

```
index.html            כל העיצוב והלוגיקה
sw.js                 service worker — תמיכה באופליין
manifest.webmanifest  הגדרות התקנה כאפליקציה
data/days.json        המסלול — ימים, תחנות, קואורדינטות, אופי היום
data/tasks.json       משימות פתוחות וסגורות
data/flights.json     קטעי הטיסה ורשימת הבדיקות
data/emergency.json   חירום, תדלוק ואזורי זמן
data/places.json      רקע על כל אתר — משויך ליום דרך שדה iso
data/packing.json     רשימת הציוד — סעיפים, כמויות ומה לא להביא
data/expenses.json    הוצאות מהקבלות
images/               איור נוף לכל יום
icons/                אייקונים למסך הבית ולדוק
tools/sync.sh              commit + push לאתר החי
tools/extract_receipts.py  מחלץ סכומים ממיילים של קבלות
tools/make-icons.sh        מרנדר מחדש את האייקונים מהלוגו
tools/make_day_art.py      מייצר מחדש את איורי הימים
```

**כדי לשנות תוכן — עורכים את קבצי ה‑JSON, לא את `index.html`.**

### תמונות לימים

לכל יום יש איור וקטורי שנוצר ב‑`tools/make_day_art.py`. הם נבחרו במקום צילומים כי המאגר ציבורי ורישוי תמונות בו הוא בעיה.

להחלפה בתמונה אמיתית: לשים את הקובץ ב‑`images/` ולהוסיף לאותו יום ב‑`days.json`:

```json
"img": "images/yosemite-valley.jpg"
```

מומלץ WebP או JPEG ברוחב ~1200px ומתחת ל‑150KB, ולהוסיף את הנתיב ל‑`SHELL` שב‑`sw.js` כדי שייכנס למטמון האופליין.

---

## איך זה מתעדכן

התיקייה הזו היא מאגר git, וכל commit ל‑`main` מתפרסם ב‑GitHub Pages תוך כדקה.

```bash
./tools/sync.sh
```

בנוסף רצה משימה מתוזמנת שלוש פעמים ביום (08:07, 14:07, 20:07) שסורקת את תווית `USA TRIP` ב‑Gmail, מעדכנת את `data/expenses.json` ו‑`data/tasks.json`, ודוחפת גם שינויים שנעשו כאן בתיקייה.

### תצוגה מקומית

הדפים טוענים JSON ב‑`fetch`, אז פתיחה ישירה ב‑`file://` לא תעבוד:

```bash
python3 -m http.server 8899
```

---

## התקנה כאפליקציה

**אייפון** — לפתוח בספארי (לא בכרום), כפתור השיתוף ← **הוספה למסך הבית**.
**מק** — ספארי, תפריט File ← **Add to Dock**. בכרום: אייקון ההתקנה בשורת הכתובת.

האייקון נוצר מ‑`tools/icon-source.html` בעזרת headless Chrome. אם משנים את הלוגו ב‑`index.html`, לעדכן שם את אותם צבעים ולהריץ `./tools/make-icons.sh`.

---

## פרטיות

המאגר ציבורי, ולכן **אין באתר מספרי אישור הזמנה, קודים סודיים או ארבע ספרות של כרטיסי אשראי**.
סכומי הכסף כן מוצגים. כשמוסיפים תוכן חדש — לא להכניס מספרי אישור.
